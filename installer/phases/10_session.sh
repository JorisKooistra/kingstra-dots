#!/usr/bin/env bash
# =============================================================================
# Fase 10_session — Sessionlaag (lock, idle, SDDM)
# =============================================================================
# STATUS: stub — wordt ingevuld in de bijbehorende projectfase
# =============================================================================

phase_run() {
    log_warn "Fase '10_session' is nog niet geïmplementeerd."
    log_info  "  Doel: Sessionlaag (lock, idle, SDDM)"
    log_info  "  Zie: docs/architecture.md voor de scope van deze fase."
}
