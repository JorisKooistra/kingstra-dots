import QtQuick
import Quickshell

// Compatibility entrypoint: some services still launch ~/.config/quickshell/shell.qml.
// Keep this file valid and intentionally inert; TopBar/Main are started separately.
ShellRoot {
}
