# =============================================================================
# 20-input.conf — Invoer (toetsenbord, muis, touchpad)
# =============================================================================

input {
    kb_layout  = us
    kb_options =                 # Bijv. "grp:alt_shift_toggle" voor meerdere talen

    follow_mouse   = 1
    mouse_refocus  = true
    sensitivity    = 0           # -1.0 tot 1.0; 0 = geen versnelling
    accel_profile  = flat

    touchpad {
        natural_scroll       = true
        disable_while_typing = true
        tap-to-click         = true
        tap-and-drag         = true
        scroll_factor        = 0.5
        clickfinger_behavior = true
        middle_button_emulation = false
    }
}

# Touchpad-gebaren — nieuwe syntax sinds Hyprland 0.51
# 3-vinger horizontaal vegen wisselt werkruimte
gesture = 3, left,  workspace, +1
gesture = 3, right, workspace, -1

cursor {
    no_warps          = true
    inactive_timeout  = 10       # Cursor verbergen na 10 seconden inactiviteit
    hide_on_key_press = false
}
