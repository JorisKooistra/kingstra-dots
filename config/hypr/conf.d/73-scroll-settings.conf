# =============================================================================
# 73-scroll-settings.conf — Scroll-tuning overrides
# =============================================================================
# Aangepast via de Settings-popup.
# =============================================================================

input {
    scroll_factor = 1.45

    touchpad {
        scroll_factor = 0.55
    }
}
