# =============================================================================
# 70-autostart.conf — Autostart
# =============================================================================
# Alle Kingstra-owned sessieprocessen lopen via één entrypoint.
# Dit houdt installatie en deinstallatie symmetrisch.
# =============================================================================

exec-once = ~/.local/bin/kingstra-session-start
